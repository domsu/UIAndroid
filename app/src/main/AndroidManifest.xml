<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uiandroid.examples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BrowseActivity"
            android:label="@string/app_label"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sample.RevealCustomColorActivity"
            android:label="@string/reveal_effect_label" />

        <activity
            android:name=".sample.SlideInOnAppStartActivity"
            android:label="@string/slide_in_on_start_label" />

        <activity android:name=".sample.assetreduction.AssetReductionActivity"
            android:label="@string/asset_reduction_label" />

        <activity android:name=".sample.viewlayers.ViewLayersActivity"
            android:label="@string/view_layers"/>
    </application>

</manifest>
